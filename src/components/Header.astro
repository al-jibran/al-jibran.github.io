<header
  class="border-b border-[var(--border)]"
>
  <div class="flex">
    <!-- Left side -->
    <div
      class="hidden xl:flex w-[280px] shrink-0 items-center border-r border-[var(--border)] px-8 py-5"
    >
      <h1
        class="text-3xl tracking-tight"
      >
        JIBRAN DEV
      </h1>
    </div>

    <!-- Right side -->
    <div
      class="flex flex-1 items-center justify-between px-10 py-5 lg:px-20"
    >
      <div
        class="text-sm text-[var(--muted)]"
      >
        Thoughts on software engineering,
        system design and solving
        difficult problems.
      </div>

      <div
        class="flex items-center gap-5 text-sm text-[var(--muted)]"
      >
        <button
          class="h-8 w-8 rounded-full border border-[var(--border)] transition hover:bg-black hover:text-white"
        >
          ☼
        </button>
      </div>
    </div>
  </div>
</header>